# Core modeling (src/)
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
lightgbm>=4.0
catboost>=1.2
scipy>=1.11

# Post-competition atlas scaling study (experiments/atlas_loso_testbed.py)
anndata>=0.10
matplotlib>=3.7

# LLM reranker research only (experiments/llm_reranker/) — runs a LOCAL model via Ollama.
# Install Ollama separately (https://ollama.com) and `ollama pull qwen3:8b`.
# No Python API key is used; nothing leaves the machine.
